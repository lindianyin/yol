# Generated by vc2mak. (c) 2012 simon<senoutouya@gmail.com>.
# Project - team_gc

# 缺省配置
ifndef CFG
	CFG=debug
endif

# 编译器标记（若要做x86、x64的cross-compile请指定PLAT参数）
CC=gcc
CFLAGS=-pthread $(PLAT)
CXX=g++
CXXFLAGS=$(CFLAGS)
LD=$(CXX)
LDFLAGS:=$(CFLAGS)
AR=ar
ARFLAGS=rcs

# 工程配置表
ifeq "$(CFG)" "debug"
	CFLAGS+= -pipe -fpic -Wall -Wno-unknown-pragmas -Wno-unused -Wno-reorder -O0 -fexceptions -g -fstack-protector  -D_DEBUG -DGAME_CENTER  -DGCMOUDLE_EXPORTS -I. -I../../include -I../../include/serverbase -I../../../LibQrt/Include -I../../../devenv/include
	LDFLAGS+= -L../../../LibQrt/Lib/Win32 -L../../../devenv/lib/debug -L../../lib/debug -shared -shared-libgcc
	LIBS=-Wl,-rpath,'$$ORIGIN' -Wl,--start-group -lonlinegamebased -lcored -lmysqlclient -Wl,--end-group -lm -ldl -lrt
	TARGET=../../product/debug/gamecenter/libteam_gc.so
	VC_CONF_TYPE=typeDynamicLibrary
endif

ifeq "$(CFG)" "release"
	CFLAGS+= -pipe -fpic -Wall -Wno-unknown-pragmas -Wno-unused -Wno-reorder -O2 -fexceptions -g -fstack-protector  -DNDEBUG   -DGCMOUDLE_EXPORTS -I. -I../../include -I../../include/serverbase -I../../../base/include -I../../../devenv/include
	LDFLAGS+= -L../../../base/lib/release/linuxso -L../../../base/lib/release -L../../../devenv/lib/release -L../../lib/release -shared -shared-libgcc
	LIBS=-Wl,-rpath,'$$ORIGIN' -Wl,--start-group -lonlinegamebase -ldevbase -lcommon -lengine -lmysqlclient -Wl,--end-group -lm -ldl -lrt
	TARGET=../../product/release/gamecenter/libteam_gc.so
	VC_CONF_TYPE=typeDynamicLibrary
endif

.PHONY: all
all: $(TARGET)

ifndef TARGET
	@echo "无编译目标！unrecognized \$$CFG '$(CFG)'"
	@exit 1
endif

%.o: %.c
	$(CC) $(CFLAGS) -o $@ -c $<
%.o: %.cc
	$(CXX) $(CXXFLAGS) -o $@ -c $<
%.o: %.cpp
	$(CXX) $(CXXFLAGS) -o $@ -c $<
%.o: %.cxx
	$(CXX) $(CXXFLAGS) -o $@ -c $<

MODULE= \
	./team/teammodule_gc.cpp \
	./team/teammodule_gc.h 

LOGIC= \
	./team/TeamCenter.cpp \
	./team/TeamCenter.h 

SRCS=$(MODULE) $(LOGIC) 

OBJS=$(patsubst %.cxx,%.o,$(patsubst %.cpp,%.o,$(patsubst %.cc,%.o,$(patsubst %.c,%.o,$(filter %.c %.cc %.cpp %.cxx,$(SRCS))))))

ifeq "$(VC_CONF_TYPE)" "typeDynamicLibrary"
$(TARGET): $(OBJS)
	mkdir -p "$(dir $(TARGET))"
	$(LD) $(LDFLAGS) -o $@ $(OBJS) $(LIBS)
endif

.PHONY: clean
clean:
	-rm -f $(OBJS) team_gc.dep  ../../product/debug/gamecenter/libteam_gc.so ../../product/release/gamecenter/libteam_gc.so

.PHONY: depends
depends:
	-$(CXX) $(CXXFLAGS) -MM $(filter %.c %.cc %.cpp %.cxx,$(SRCS)) > team_gc.dep

-include team_gc.dep

